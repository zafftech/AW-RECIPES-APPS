<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logoapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginDanRegisterVolleyMySql"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Login Activity -->
        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name=".Register"
            android:exported="false" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Add these missing activities -->
        <activity android:name=".Ayamg" />
        <activity android:name=".kulitayam" />
        <activity android:name=".Waffle" />
        <activity android:name=".SosejJumbo" />
        <activity android:name=".about" />
        <activity android:name=".UserGuide" />
        <activity android:name=".KepakAyamActivity" />



    </application>

</manifest>
